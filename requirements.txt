# Lean gate (charter 2026-06-16): Python dependency manifest.
#
# This repo's APPLICATION is pure Java/Android and currently contains NO Python
# (the legacy quality-zero-platform helper scripts under scripts/ were removed in
# the lean-gate migration; only the bash scripts/verify remains).
#
# This file is intentionally dependency-free. It is retained as forward-looking
# scaffolding: the lean reusable quality workflow's `actions/setup-python`
# (cache: pip) step expects a manifest (requirements.txt or pyproject.toml), and
# osv-scanner uses it as a (currently empty) dependency source. A requirements.txt
# does NOT trigger the template's Python coverage lane (that keys off
# pyproject.toml / setup.cfg / tox.ini), so no coverage gate is introduced.
#
# Add real pins here if/when application Python with third-party deps is added.
