android.useAndroidX=true
android.enableJetifier=true

# Raise the Gradle/JVM heap so JetifyTransform of large artifacts
# (e.g. org.robolectric:nativeruntime-dist-compat) does not OOM during
# :app:testDebugUnitTest on CI.
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g
